{% extends 'installation/base.html.twig' %}

{% block title %}Configurations{% endblock %}

{% block body %}
    <h2 class="border-bottom h4">Configuration</h2>
    <p>
        Renseignez les informations de configuration. Seuls les champs suivis d'une étoile sont obligatoires. 
        Vous pourrez modifier à tout moment ces informations depuis la page de configuration de l'administration.
    </p>

    {% include "Admin/settings/_settings_form.html.twig" %}
{% endblock %}

{% block javascripts %}
    <script>
        function togglePassword(targetId) {
            const input = document.querySelector(`#${targetId}`);
            if (input.type === 'password') {
                input.type = 'text';
            } else {
                input.type = 'password';
            }
        }
    </script>
{% endblock %}