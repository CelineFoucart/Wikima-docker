{% extends 'base.html.twig' %}

{% block title %}Uploader une images{% endblock %}

{% block body %}
    <header class="bg-default-light border-bottom">
        <div class="container py-4">
            <div class="item-heading d-flex justify-content-between align-items-center">
                <h1 class="mb-1"><i class="bi bi-image me-1"></i> Uploader une image</h1>
                <a href="{{ path('app_edit_image_index') }}" class="btn btn-outline-primary">
                    Retour à l'index
                </a>                
            </div>
        </div>
    </header>

    {{ include('components/layout/_editor_form.html.twig', {button_label: 'Uploader'}) }}
{% endblock %}
