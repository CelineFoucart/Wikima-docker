{{ form_start(form) }}
    <div class="search-row">
        {{ form_widget(form) }}
        <button type="submit" class="search-btn"><i class="bi bi-search"></i></button>
    </div>
{{ form_end(form) }}