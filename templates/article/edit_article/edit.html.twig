{% extends 'base.html.twig' %}

{% block title %}Editer un article{% endblock %}

{% block body %}
    <header class="bg-default-light border-bottom">
        <div class="container py-4">
            <div class="item-heading d-flex justify-content-between align-items-center">
                <h1 class="mb-1"><i class="bi bi-file-earmark-text me-1"></i> Editer un article</h1>
                <a href="{{ path('app_edit_article_index') }}" class="btn btn-outline-primary">
                    Retour à l'index
                </a>                
            </div>
        </div>
    </header>

    {{ include('components/layout/_editor_form.html.twig', {button_label: 'Editer'}) }}
{% endblock %}
